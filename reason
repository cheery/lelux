/init
  /bin/busybox and it's binaries
  /bin/mount
  /bin/sh
  /bin/hostname
  /bin/ip
  /bin/cat
  /sbin/getty
    /bin/login
      /etc/group
      /etc/passwd
      /bin/ash
        /etc/profile
    /dev
    /proc
    /var/run

/bin/nslookup /bin/wget, networking software that's compiled against glibc (only needed to get DNS working)
  /etc/resolv.conf
  /etc/hosts
  /etc/nsswitch.conf
  /lib/libnss_files.so.2
  /lib/libc.so.6
  /lib/libresolv.so.2
  /lib/ld-linux-x86-64.so.2
  /lib/libnss_dns.so.2

/sys is mounted, but probably not used. Just for convenience if something needs it.
